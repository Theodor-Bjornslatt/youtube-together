import styled from 'styled-components'

import { TextInputContainer } from '../TextInput/textInput.styled'

export const TextAreaInputContainer = styled(TextInputContainer)`
  .input {
    resize: none;
    max-height: 72px;
  }
`
